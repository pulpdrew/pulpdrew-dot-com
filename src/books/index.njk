---
layout: base
tab: books
title: Books
---

<div class="mx-1 lg:mx-auto w-12/12 lg:w-8/12 lg:mt-10">

  <h2 class="text-xl mb-2 text-center">Some books that I've read.</h2>
  <p class="font-thin mb-8 text-center">And some of my thoughts about them.</p>

  {%- for book in collections.book | reverse -%}

  <!-- Book Summary -->
  <article class="flex flex-col p-4 my-6 lg:mx-0 shadow-md border-t-2 border-blue-800">

    <div class="flex justify-between mb-1 align-bottom flex-wrap">
      <h2 class="text-xl font-semibold mr-5">{{ book.data.title }}</h2>
      <time>{{ book.data.date.toLocaleDateString() }}</time>
    </div>
    <span class="font-thin">By {{ book.data.author }}</span>
      
    <div class="mt-2 md-content">{{ book.templateContent | safe }}</div>

    <div class="flex justify-start flex-wrap">
      <span>Rating: {{ book.data.rating }} / 10</span>
      <a href="{{ book.data.url }}" class="ml-6 text-blue-800">Get Book</a>
    </div>
  </article>

  {%- endfor -%}
</div>